import { Component, render } from 'preact';
import { useState } from "preact/hooks";

class Page extends Component {
	render() {
		return (
			<div class="container">
				<h1>Hi</h1>
			</div>
		);
	}
}

export default Page;
